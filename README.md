# Practica spam
##De que trata la practica
La practica trato de crear un un detector de spam esto se logra atravez de un modelo que entrenamos y predice si el texto es spam o no
##como?
atravez del cmd creamos una carpeta nueva
crear un entorno virtual
cd ir a carpeta
mkidr (crea una carpeta)

python -m venv venv 
venv\Scripts\activate
para que otras programas no interfieran con el codigo
se puede utilizar para trabajar en conjunto con otros equipos
gestor de paquetes
	pip install numpy
	pip list 	(ver los paquetes instalados)
congelar dependencias
pip  freeze 
pip install -r (instala las paqutererias del proyecto)
math operaciones

Linus Torvalds
creador de linux	git sistema de control permite gestionar el codigo
instalar git
git config --

github		portafolio de proyectos
es la nube de proyectos de git
cuenta personal y institucional


README.md
Documento de instrucciones eh interaccione 
add .gitignore 	
add license 	Derechos de autor
para instalar mismas librerias y versiones
pip install -r requeriments.txt

licensia MIT GNU Permite comparti codigo pero con mencion de autor
proyecto de softwer
working directory directorio local
git add stagin area ver lo que se modifica y se sube
git commit repository nube
cambios atraves de ficheros o carpetas
git commit -m nueva version del proyecto

.csv archivo separado por comas

tkinder
biblioteca en python sirve para crear una interfaz con python
ventana simple codigo
import tkinter as tk 

ventana = tk.Tk()
ventana.title("ventana simple")

ventana.mainloop()

guardar cambios
git add .
git commit -m "19 de noviembre"

##Trabajo futuro (mejoras)
Me gusto como nos enseñaron a crear proyectos y a quitarnos el miedo de usar el cmd
me gusta en el orden en el que se fue dando el proyecto asi me doy una idea de como crear los mios
